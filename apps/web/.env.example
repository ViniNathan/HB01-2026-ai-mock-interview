# Frontend (Next.js) — copy to apps/web/.env.local for local development.

# Base URL of the Express/tRPC backend (must match server PORT). Ex.: http://localhost:3000
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
