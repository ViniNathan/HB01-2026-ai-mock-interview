generator client {
  provider     = "prisma-client"
  output       = "../generated"
  moduleFormat = "esm"
  runtime      = "bun"
}

datasource db {
  provider = "postgresql"
}

// Imports user and refresh token models
// Models are defined in user.prisma
// DATABASE_URL is configured via prisma.config.ts
